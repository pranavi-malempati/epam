package cc;
public class compoundint extends simpleint {
public static void Compoundint(int principal, int rate_of_interest, int time)  {
	int Compound_Interest = 0;
	int k =(1 + rate_of_interest);
	Compound_Interest =(principal) * time - principal;
	intrest2(Compound_Interest);
}

}
